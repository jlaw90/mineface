- unless @devs.empty?
  %span.pad-horizontal
    %i.icon-time
    ="#{t('title.uptime')}: #{time_ago_in_words(Time.now.to_i - @sum[:elapsed])}"
  |
  %span.pad-horizontal
    %i.icon-signal
    ="#{t('title.speed')}: #@speed"